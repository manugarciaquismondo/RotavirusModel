% RetrieveTotalStore

SumTotalStore

Store_unnormalized_i0 = Store_i0;
Store_unnormalized_i1 = Store_i1;
Store_unnormalized_i2 = Store_i2;
Store_unnormalized_i3 = Store_i3;
Store_unnormalized_i4 = Store_i4;

Store_m0(:,:) = Store_m0(:,:) / TotalStore(:,:);
Store_i0(:,:) = Store_i0(:,:) / TotalStore(:,:);
Store_i1(:,:) = Store_i1(:,:) / TotalStore(:,:);
Store_i2(:,:) = Store_i2(:,:) / TotalStore(:,:);
Store_i3(:,:) = Store_i3(:,:) / TotalStore(:,:);
Store_i4(:,:) = Store_i4(:,:) / TotalStore(:,:);
Store_r0(:,:) = Store_r0(:,:) / TotalStore(:,:);
Store_r1(:,:) = Store_r1(:,:) / TotalStore(:,:);
Store_r2(:,:) = Store_r2(:,:) / TotalStore(:,:);
Store_r3(:,:) = Store_r3(:,:) / TotalStore(:,:);
Store_r4(:,:) = Store_r4(:,:) / TotalStore(:,:);
Store_s0(:,:) = Store_s0(:,:) / TotalStore(:,:);
Store_s1(:,:) = Store_s1(:,:) / TotalStore(:,:);
Store_s2(:,:) = Store_s2(:,:) / TotalStore(:,:);
Store_s3(:,:) = Store_s3(:,:) / TotalStore(:,:);
Store_s4(:,:) = Store_s4(:,:) / TotalStore(:,:);
Store_v1(:,:) = Store_v1(:,:) / TotalStore(:,:);
Store_v2(:,:) = Store_v2(:,:) / TotalStore(:,:);
Store_v3(:,:) = Store_v3(:,:) / TotalStore(:,:);
Store_v4(:,:) = Store_v4(:,:) / TotalStore(:,:);